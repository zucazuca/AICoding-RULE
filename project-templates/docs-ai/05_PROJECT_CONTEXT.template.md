<!--
  AICoding-RULE 项目模板：docs/ai/05_PROJECT_CONTEXT.md
  本文件全部属于项目层，无受控区块。
  结构来源：原始 Project Context 填空模板 + 实践演化出的"当前事实文档"十四节结构。
  治理要求（见 core/05_DOCUMENT_GOVERNANCE_RULES.md）：
  只保存当前有效上下文；旧结论原位替换；不做里程碑流水账；防膨胀。
-->
# {{PROJECT_NAME}} 当前项目上下文

> 本文档是 AI Coding Agent 的当前项目事实文档。
>
> 优先级低于阅读规范，高于执行规范、测试规范和输出规范。
>
> 任何 AI 开始任务前必须先阅读本文档。

------

## 1. 文档定位与更新时间

- 定位：**只保存当前有效上下文**，不记录里程碑流水账，不按日期追加任务完成记录。
- 更新时间：{{DATE}}。
- 同一事实只保留一份当前结论；旧结论失效时必须原位替换或删除。
- 历史内容见归档：`docs/ai/archive/`（仅追溯用，不是当前事实）。

------

## 2. 当前项目目标与范围

- 项目是什么：{{一句话定位，例如：CRM / ERP / RAG 客服 / 自动化工具}}
- 当前阶段目标：{{当前阶段}}
- 当前范围内 / 范围外：{{范围边界，含已确认取消的内容}}

------

## 3. 当前系统组件与服务边界

| 组件 | 端口 / 位置 | 职责 |
|---|---|---|
| {{组件1}} | {{端口}} | {{职责}} |

职责红线：

- {{谁是某类数据的权威源}}
- {{什么不可信（如前端传入的身份上下文）}}

------

## 4. 当前环境与部署边界

- 环境清单（dev / staging / production / LAN 等）与各自入口：{{...}}
- 部署编排文件职责（哪个是生产入口、哪个禁止单独运行）：{{...}}
- 环境变量模板：{{...}}

------

## 5. 当前鉴权、权限与隔离

- 鉴权方式与默认值（代码默认 vs 生产要求）：{{...}}
- 角色与权限模型：{{...}}
- 数据隔离边界（租户 / 商户 / 用户）：{{...}}
- 业务语义映射（current_user / owner / admin 等在本项目的真实含义）：{{...}}

------

## 6. 当前数据库与迁移状态

- 数据库类型与当前主库：{{...}}
- 迁移机制与当前进度：{{...}}
- 强制注意事项（连接坑、禁 create_all 之类）：{{...}}

------

## 7. 当前核心业务流程

```text
{{流程图或调用链：入口 → 核心逻辑 → 数据写入 → 出口}}
```

------

## 8. 当前模块地图与核心业务对象

- 模块 → 职责 → 位置：{{...}}
- 核心业务对象与状态机：{{...}}

------

## 9. 当前安全边界与门禁

<!-- 除非用户明确批准不得放宽的运行保护 -->

1. {{...}}

------

## 10. 当前前端 / 客户端能力

- {{位置、页面清单、配置约束（如 TS 配置锁定项）}}

------

## 11. 当前已完成能力（提炼）

- {{只列能力结论，不列提交号 / 测试数量 / 文件清单}}

------

## 12. 当前未完成事项

| 事项 | 状态 |
|---|---|
| {{...}} | {{待做 / 阻塞 / 待审批 / 已延期}} |

------

## 13. 当前风险与强制注意事项

### 13.1 部署 / 生产

1. {{...}}

### 13.2 测试与回归（含已知环境性失败基线）

1. {{全量回归命令与忽略项}}
2. {{已知环境性失败及规避方式；回归以"零新增失败"放行}}

### 13.3 附加必读登记

| 修改范围 | 必读文档 |
|---|---|
| {{某模块}} | {{某验收 / 边界文档}} |

------

## 14. 后续执行窗口的推荐入口

| 场景 | 入口 |
|---|---|
| {{场景}} | `{{路径}}` |

专题目录按需读取，禁止默认遍历整个 `docs/ai`。
